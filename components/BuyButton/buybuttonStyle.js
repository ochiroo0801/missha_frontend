import styled from "styled-components";

const Div = styled.div`
  .buy {
    background: #000;
    color: #fff;
    border-radius: 2px;
    padding: 8px;
    font-size: 18px;
    font-weight: 700;
  }
`;

export default Div;
